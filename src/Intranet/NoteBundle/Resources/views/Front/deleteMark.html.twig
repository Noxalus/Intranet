{% extends 'IntranetCoreBundle:Core:layout.html.twig' %}

{% block content %}
<h2>Vous êtes sur le point de supprimer une note.</h2>
 
  <p>
      Cette action est irréversible. Êtes-vous sur de vouloir supprimer la note de <b>{{ mark.user.firstName }} {{ mark.user.lastName }}</b> (<i>{{ mark.value }}/{{ mark.exam.maxMark }}</i>) pour l'exam de <b>{{ mark.exam.courseType.name }}</b> ?
  </p>
 
  {# On met l'id de l'article dans la route de l'action du formulaire #}
  <form method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <input type="submit" value="Supprimer" class="btn btn-danger" />
  </form>
{% endblock %}